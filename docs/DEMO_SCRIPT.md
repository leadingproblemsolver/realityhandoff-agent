# Final <3 minute demo script

**0:00–0:18 — Thesis.** “DataHub gives agents context. Reality Handoff makes actions proof-carrying across sessions.” Show: `MCP → evidence → reality → gate → contract → approve → act → verify → handoff → recover`.

**0:18–0:38 — P0/P1 live context.** Show MCP preflight tool discovery. Run the configured showcase-ecommerce target and expose evidence IDs for entity/schema plus upstream/downstream lineage.

**0:38–0:58 — Negative control.** Run `Fix the revenue definition...`. Show `NEEDS_HUMAN` and zero mutation. Emphasize: LLM interpretation cannot override the deterministic gate.

**0:58–1:38 — Real work.** Run valid target task. Show ExecutionContract: one target, `update_description`, append-only, handoff disclosed, stop conditions. Show the human approval interrupt; approve. Show DataHub mutation result.

**1:38–2:02 — Proof, not optimism.** Show independent `get_entities` re-read. The exact contract marker exists on the target and verifier returns `VERIFIED`.

**2:02–2:30 — Durable continuity.** Show `save_document` Decision write and the graph's independent handoff re-read. Then start a fresh process/thread with no conversation history and run `reality-handoff recover ...`. Show recovered evidence IDs, decision, verification and next safe action.

**2:30–2:52 — Judgeability.** Open `/demo`, run the positive replay and ambiguity control, then show `pytest` results. Mention that replay is explicitly labeled non-live.

**2:52–2:59 — Close.** “The artifact isn't another catalog chatbot. It is a bounded execution and continuity layer on top of DataHub's real organizational context.”
